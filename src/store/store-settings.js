import firebaseInstance from '../../middleware/firebase'
import {LocalStorage} from 'quasar'

const state = {
  settings: {

  }
}

const mutations = {
}

const actions = {
}

const getters = {

}

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
